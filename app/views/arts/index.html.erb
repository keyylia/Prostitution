<p id="notice"><%= notice %></p>

<h1>Prostitution</h1>

<table class="table", class="pager">
  <thead>
    <tr>
      <th>Auteur</th>
      <th>Titre</th>
      <th>Date</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @arts.each do |art| %>
      <tr>
        <td><%= art.author %></td>
        <td><%= art.title %></td>
        <td><%= art.date %></td>
        <td><%= art.description %></td>
        <td><%= image_tag art.image, class: 'img-fluid' %></td>
        <td><%= link_to 'Show', art %></td>
        <td><%= link_to 'Edit', edit_art_path(art) %></td>
        <td><%= link_to 'Destroy', art, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Art', new_art_path %>
